{
  "name": "antenna-studio-frontend",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "description": "Frontend sources for Antenna Studio. This package is no longer built by Node; the Go backend (backend/internal/assets) bundles the TypeScript tree in-process via esbuild's Go library. `npm install` is only used to populate node_modules so the Go bundler can resolve imports.",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@react-three/drei": "^9.105.0",
    "@react-three/fiber": "^8.16.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "recharts": "^2.12.0",
    "three": "^0.163.0",
    "uuid": "^9.0.1",
    "zustand": "^4.5.2"
  },
  "devDependencies": {
    "@types/react": "^18.3.1",
    "@types/react-dom": "^18.3.0",
    "@types/three": "^0.163.0",
    "@types/uuid": "^9.0.8",
    "typescript": "^5.4.5"
  }
}
